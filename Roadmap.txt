TODO v0.0.1:
- GUI -> onClick, onEvent vs TCPServer -> onConnect, onData.
- DoubleMap -> Int64Map? UllMap? (ncpp-system)? Или может структурку сделать?
-- Не разбрасываться noexept
- HTTP: вспомогательные функции: cookie parser, qparse, etc.
-- Req: waitbody().
- Оптимизация и разделение base: min-base(print, etc...)

- Stream
-- Socket
--- UnixStream (+/-)
--- ICMPSocket: inet_addr() only accept IPv4.
-- FileStream
- AsyncIO
-- StreamPool
--- SocketPool
-- File Async
-- TCP Server (+/-)
--- HTTP (+/-)
---- AutoIndex
-- Promise?
- System
-- CPU getCoresTime()
-- Disk (wmic -> WinApi)
- Date
- Threads (+/-)
-- Thread (Event) Pool?
-- ncpp::Thread::Signal.emit() (EventEmiter?) (condition_variable)?
-- futex?
-- ??
- Object (+/-)
-- BSON/CBOR? (+/-)
- Encryption
-- TLS
--- RSA/DH
---- BigInt (!)
--- X.509/ASN.1
-- SSH?
- GUI
-- Минимальный набор(Кнопки, Input, Label, ...);
-- Image (Win: GDI/GDI+, X11: XPutImage/XRender(GPU?), Wayland: ?)(Cairo: example)
--- BMP parser? (Подбор оптимальный осруктуры для быстрой обработки? Лучшие практики Cairo)
- const std::string&? -> CString?
- Закрыть все TODO?
- Разобрать реализацию sqlite3.c?
- clang 3.5 support, Linux 3.10.0 and 2.6.18 (Debian4)
-- Поддержка gcc3_winxpdef.h
-- Сделать библиотеку независимой от libstdc++?
--- Поддержка макроса NO_STDCPP, с ручными имплементациями std::vector (свой аллокатор?), std::string, std::map, std::unordered_map под musl.
-- CI/CD?
- Упаковать либу в .deb пакет.

Если вы используете строгие режимы компиляции или специфические флаги компилятора, которые исключают функции стандартной библиотеки (например, для защиты от ошибок или для контроля размера кода), то memmove() может быть недоступна.

- OpenPGP?

https://en.wikipedia.org/wiki/Windows_Native_API
https://emn178.github.io/online-tools/